{
  "name": "ticketa",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "contract:compile": "node scripts/compile-contract.mjs",
    "contract:test": "npm run contract:compile && node scripts/check-contract.mjs",
    "test:ui": "node scripts/check-ui.mjs",
    "deploy:testnet": "npm run contract:compile && node scripts/deploy-testnet.mjs",
    "verify:testnet": "node scripts/verify-contract.mjs",
    "check:testnet": "node scripts/check-network.mjs"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2",
    "@zxing/browser": "0.1.5",
    "ethers": "^6.17.0",
    "lucide-react": "^0.468.0",
    "qrcode": "^1.5.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "solc": "0.8.36",
    "vite": "^6.0.0"
  },
  "overrides": {
    "tmp": "^0.2.7"
  }
}
